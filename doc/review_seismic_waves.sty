% Geometry package for page layout

%\usepackage[body={8.5in, 11in}, headheight=1mm]{geometry}
\usepackage{geometry}
\geometry{
  top=1in,    % Margen superior
  bottom=1in, % Margen inferior
  left=1in,   % Margen izquierdo
  right=1in   % Margen derecho
}

% Caption package for figure captions
\usepackage[margin=1cm]{caption}
\captionsetup[figure]{font={sf, small}, labelfont={normal, small, bf}, labelsep=period}
\captionsetup[table]{font={sf, small}, labelfont={normal, small, bf}, labelsep=period}



% Packages for mathematical symbols and graphics
\usepackage{amsmath} % For \boldsymbol
\usepackage{amssymb} % For \mathbb
\usepackage{graphicx} % For including graphics

% Additional packages for color, boxes, headers, and floats
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{fancyhdr}
\usepackage{float} % For exact figure placement
\usepackage{fontawesome}
\usepackage{fancyvrb} % This package helps to use \Verbatim
\usepackage{academicons}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{graphicx}
\usepackage{longtable} 
% Bibliography and citation settings
\usepackage{natbib}
\bibliographystyle{unsrtnat}
\setcitestyle{square, numbers}

% Definition of \iconbox command
\newtcbox{\iconbox}[1][]{
  nobeforeafter, tcbox raise base, 
  boxrule=0.4pt, top=0pt, bottom=-1pt,
  right=0.1pt, left=0.1pt, arc=1pt, boxsep=1pt,
  fontupper={\tiny \sffamily}, before upper={\vphantom{dlg}},
  colframe=white, colback=black!10!white, coltext=black, #1
}

% Packages for drawing lines and drop caps
\usepackage{tikz} % For drawing lines
\usepackage{lettrine} % For drop caps

% Redefining citation commands with gray text
\renewcommand{\citep}[2][]{\textcolor{gray}{(\citeauthor{#2}, \citeyear[#1]{#2})}}
\renewcommand{\citeauthoryear}[2][]{\textcolor{gray}{\citeauthor{#2} (\textcolor{gray}{\citeyear[#1]{#2}})}}

% Font settings
\usepackage{fontspec}
\setmainfont{newbask}[
  Path = ./fonts/,
  UprightFont = *n.ttf,
  BoldItalicFont = *z.ttf,
  BoldFont = *b.ttf,
  ItalicFont = *i.ttf,
  BoldFeatures = {SmallCapsFont = {nbaskervillecapbold}},
  ItalicFeatures = {SmallCapsFont = {lmromancaps10-oblique-webfont}},
  SmallCapsFont = {lmromancaps}
]

% Hyperref settings for clickable links
\usepackage{hyperref}
\hypersetup{
  colorlinks   = true,
  citecolor    = [rgb]{0.0, 0.0, 0.5}, % Darker blue
  linkcolor    = [rgb]{0.5, 0.0, 0.0}, % Darker red
  urlcolor     = gray
}

% Fancyhdr settings for custom headers
\pagestyle{fancy}
\fancyhf{} % Clear default header and footer
\renewcommand{\headrulewidth}{0pt} % Remove header line

% Header configuration
\fancyhead[LE]{\thepage\, / \textit{Machine Learning Modeling for Seismic Waves}} % Even pages header
\fancyhead[RO]{} % Odd pages header

% Page numbering settings
\pagenumbering{arabic} % Arabic page numbers
\setcounter{page}{1} % Start page numbering at 1

% Keywords command
\providecommand{\keywords}[1]
{
  \small	
  \textbf{{Keywords:}} #1
}

\raggedbottom

\AtBeginDocument{
    \setlength{\abovedisplayskip}{-1pt plus -1pt minus -1pt}
    \setlength{\belowdisplayskip}{10pt plus 3pt minus 1pt}
    \setlength{\abovedisplayshortskip}{-1pt plus -1pt minus -1pt}
    \setlength{\belowdisplayshortskip}{10pt plus 3pt minus 1pt}
}

\bibliographystyle{unsrtnat}
\setcitestyle{square,numbers}